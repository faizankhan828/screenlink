import 'package:flutter/material.dart';

/// Root navigator for full-screen routes (chat, create project) above the shell.
final GlobalKey<NavigatorState> rootNavigatorKey = GlobalKey<NavigatorState>();
